# ContentGraph — Environment Variables
# Copy this file to .env.local and fill in values as needed.

# Optional: Set a server-side Anthropic API key.
# If set, you can modify the API route to use this instead of requiring
# users to provide their own key via the UI.
# ANTHROPIC_API_KEY=sk-ant-...
